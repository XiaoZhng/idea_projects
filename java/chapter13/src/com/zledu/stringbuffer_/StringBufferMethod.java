package com.zledu.stringbuffer_;

public class StringBufferMethod {
    public static void main(String[] args) {
        //增加：append()
        //删除：delete(11,14) 删除索引为 >11 && <14 的字符，不包含14，前闭后开
        //修改：replace(9,11,"jack") 使用 jack 替换索引9-11的字符 (前面包含后面不包含)
        //查找：indexOf() 查找指定的子串在字符串第一次出现的索引，如果找不到返回-1
        //插入：insert(9,"jack") 在索引为9的位置插入 jack ，原本索引为9的内容自动后移
        //长度：length()
    }
}
